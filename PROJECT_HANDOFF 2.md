# PROJECT_HANDOFF.md

## 项目定位

本项目是一个用于“小红书日语选题灵感获取”的工具网站。

它不是普通日语词典，也不是最终发布管理系统，而是帮助运营人员从日语词库、网络热词、历史内容反馈中发现适合做小红书内容的日语词和选题方向。

## 产品核心流程

网络热词 / 本地词库
↓
每日推荐
↓
收藏 / 我的选题池
↓
发布到小红书
↓
已发布数据复盘
↓
反哺后续推荐

## 模块分工

### 1. 每日推荐

每日推荐是系统发现热门词、优质词、适合内容化的日语词。

每日推荐不是人工审核后的最终发布池。

系统应该尽量提高每日推荐的准确性、热门度和内容价值。

每日推荐里的词不代表用户一定会发布，只代表系统认为值得用户查看。

### 2. 收藏 / 我的选题池

收藏才是人工筛选动作。

用户看到每日推荐里的词，如果觉得适合自己的小红书账号，就点击收藏。

用户真正要发在小红书的词，是从收藏 / 我的选题池里面挑选的。

收藏 / 我的选题池代表用户主观认可、可能会发布。

### 3. 候选池

候选池可以理解为系统侧的潜在选题池。

它可以包含每日推荐、历史推荐、收藏延展词、已发布内容反馈产生的相关词、未来网络热词池补充的词。

候选池不是最终发布池。

### 4. 已发布

已发布模块用于记录真正发出去的小红书内容和数据表现。

已发布数据用于复盘哪些词、标题、内容形式更适合账号，并反过来影响后续每日推荐排序。

## 当前重要技术背景

当前项目是静态前端 + Cloudflare Pages Functions。

主要文件包括：

- index.html：页面结构
- styles.css：页面样式
- app.js：主要前端逻辑
- words-data.js：当前前端词库
- shared/words-data.mjs：当前后端榜单词库
- functions/rankings.js：每日推荐榜单接口
- functions/favorites.js：收藏、状态、候选池、已发布记录同步接口
- functions/published-refresh.js：已发布数据刷新接口
- sync-config.js：同步接口配置

当前词库存在前后端两份维护的问题：words-data.js 和 shared/words-data.mjs 内容重复，后续应改为单一数据源，例如 data/words-data.json，再由脚本自动生成前端和后端兼容文件。

## 后续开发优先级

1. 统一词库维护方式，只维护 data/words-data.json
2. 增加词库校验脚本
3. 增加 package.json 和标准 npm scripts
4. 优化云端同步安全，不允许默认写入 global 数据
5. 增加导入 / 导出备份功能
6. 增加网络热词池
7. 增加 AI 清洗和热词评分
8. 再接入公开趋势源自动抓取

## 开发注意事项

- 不要把每日推荐理解成最终发布池。
- 不要要求所有进入每日推荐的词都必须人工确认。
- 人工筛选发生在收藏环节。
- 收藏 / 我的选题池才代表用户主观认可、可能会发布。
- 后续新增网络热词池、AI 清洗、推荐排序时，都要遵守这个产品逻辑。
- 不要在没有明确需求的情况下大规模重构 app.js。
- 不要破坏现有 localStorage 数据结构。
- 不要破坏 Cloudflare Pages Functions 的接口兼容性。
- 不要绕过任何平台登录、验证码或反爬机制。
- 小红书、抖音、微博等平台数据第一版应采用手动导入或合规接口，不要写强制爬虫。
