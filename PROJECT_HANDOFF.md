# Project Handoff

The current handoff document has moved to:

`docs/PROJECT_HANDOFF.md`

Please use the docs version as the source of truth. The root file is kept only for backward compatibility with older notes that linked to `PROJECT_HANDOFF.md`.
